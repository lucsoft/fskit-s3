<?xml version="1.0" encoding="UTF-8"?>
<!-- Info.plist for the ExtensionKit FSKit extension target.
     The FSKit module attributes (short name, supported resource kinds) live
     INSIDE EXAppExtensionAttributes — that's where FSKit/mount read them (verified
     against Apple's msdos and fuse-t modules). We're a device-less filesystem, so
     we accept a PathURL resource (FSSupportsPathURLs), not a block device. -->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>EXAppExtensionAttributes</key>
    <dict>
        <key>EXExtensionPointIdentifier</key>
        <string>com.apple.fskit.fsmodule</string>
        <key>FSShortName</key>
        <string>fskit-s3</string>
        <key>FSSupportsBlockResources</key>
        <false/>
        <key>FSSupportsPathURLs</key>
        <true/>
        <key>FSSupportsServerURLs</key>
        <false/>
    </dict>
</dict>
</plist>
